---
title: "CLI tool for data processing"
category: python
summary: "A command-line utility that parses logs and builds reports. Example of a Python-category project."
date: 2025-06
featured: true
tags: [cli, data, argparse]
tech: [Python, pandas, Click]
# cover: cover.png            # put a file in assets/ and uncomment
links:
  - { label: "GitHub", url: "https://github.com/username/repo", icon: github }
  - { label: "Demo", url: "https://example.com", icon: external }
# gallery:                    # screenshots (files in assets/); omit -> section hidden
#   - { src: shot1.png, caption: "Main screen" }
# video: "https://www.youtube.com/embed/VIDEO_ID"
---

## About

This is the page body in **Markdown**. Free-form text, lists, code —
everything needed to describe a specific project.

- Supports formatting
- Lists, links, images
- Code blocks with highlighting

```python
def main() -> None:
    print("Hello from the project page")
```

### Raw HTML embeds

You can drop raw HTML right into Markdown — an interactive widget, `<video>` or `<canvas>`.
That's how you add non-standard blocks the shared template doesn't cover.

> Tip: metadata (tags, stack, links) lives in the frontmatter at the top,
> and the unique content goes here in the body.