---
title: "CLI-инструмент для обработки данных"
category: python
summary: "Утилита командной строки, которая парсит логи и строит отчёты. Пример проекта категории Python."
date: 2025-06
featured: true
tags: [cli, data, argparse]
tech: [Python, pandas, Click]
# cover: cover.png            # положи файл в assets/ и раскомментируй
links:
  - { label: "GitHub", url: "https://github.com/username/repo", icon: github }
  - { label: "Демо", url: "https://example.com", icon: external }
# gallery:                    # скриншоты (файлы в assets/); если нет — блок не показывается
#   - { src: shot1.png, caption: "Главный экран" }
#   - screenshot2.png
# video: "https://www.youtube.com/embed/VIDEO_ID"
---

## О проекте

Это тело страницы на **Markdown**. Здесь свободный текст, списки, код —
всё, что нужно для описания конкретного проекта.

- Поддерживает форматирование
- Списки, ссылки, изображения
- Блоки кода с подсветкой

```python
def main() -> None:
    print("Hello from the project page")
```

### Вставка сырого HTML

Прямо в Markdown можно вставлять HTML — например интерактив, `<video>` или `<canvas>`.
Именно так добавляются нестандартные блоки, которых нет в общем шаблоне.

> Совет: метаданные (теги, стек, ссылки) живут во frontmatter сверху,
> а уникальное наполнение — здесь, в теле.
